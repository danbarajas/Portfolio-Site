---
interface Props {
    id: string;
    class?: string;
}
---

<section id={Astro.props.id} class={Astro.props.class + " flex flex-col place-items-center justify-center w-screen h-screen snap-center snap-always"}>
    <slot />
</section>