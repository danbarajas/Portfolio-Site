<div id="hero" class="relative w-screen h-screen transform-3d snap-center">
    <h1>DANIEL BARAJAS</h1>
</div>

<style>
    #hero::before {
        content: '';
        position: absolute;
        width: 100%;
        height: 100%;
        background: url('../assets/hero.png') center / cover;
        transform: translateZ(-2px) scale(2);
    }

    #hero h1 {
        position: absolute;
        font-size: 10vw;
        text-wrap: nowrap;
        text-align-last: justify;
        text-justify: inter-character;
        width: 100%;
        padding: 0 2%;
        font-weight: bold;
        color: white;
        transform: translate(-50%, -175%) translateZ(-1px) scale(1.5);
        top: 50%;
        left: 50%;
        text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3), 5px 5px 70px rgba(0, 0, 0, 0.3);
    }

    #hero::after {
        content: '';
        position: absolute;
        width: 100%;
        height: 100%;
        background: url('../assets/hero-foreground.png') center / cover;
    }
</style>